# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
data

# Vendored from vscode-material-icons (copied at setup time, not source we
# wrote) — also keeps these 900+ SVGs out of the file index, since the
# watcher respects .gitignore and these aren't "your files" to search.
public/material-icons

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
