---
description: Explain something in the reference index by always synthesizing citations, regardless of query phrasing
argument-hint: [query]
allowed-tools: mcp__reference-mcp__explain
---

Call the `mcp__reference-mcp__explain` tool with query: $ARGUMENTS and folder: ${CLAUDE_PROJECT_DIR}

Report back the citations it returns — do not use grep or Bash for this, the point of this command is to go straight through the MCP tool.
